# gaming-packages-solus.txt
# Usage: sudo eopkg install $(cat gaming-packages-solus.txt)

#!/bin/bash

gaming="steam heroic-games-launcher"
performance="mangohud mangohud-32bit goverlay"
graphics="mesa mesa-32bit vulkan vulkan-32bit"
input="steam-devices sdl2 sdl2-32bit"
audio="pipewire pipewire-alsa pipewire-pulse wireplumber alsa-utils discord"

sudo eopkg install ($gaming && $performance && $graphics && $input && $audio)
echo sudo eopkg install history | grep Installed

# === Core Platforms ===
steam
heroic-games-launcher

# === Performance Tools ===
mangohud
mangohud-32bit
goverlay

# === Graphics Stack (AMD + RADV Vulkan) ===
mesa
mesa-32bit
vulkan
vulkan-32bit

# === Controllers & Input ===
steam-devices
inputplumber
sdl2
sdl2-32bit

# === Audio (PipeWire) ===
pipewire
pipewire-alsa
pipewire-pulse
wireplumber
alsa-utils
discord

echo sudo eopkg install history | grep Installed

# === Post-Install Checks ===

# === Install ProtonUp-Qt via Flatpak === (not in repos):
# flatpak install flathub net.davidotek.pupgui2
# eduke32

# === Verify RADV Vulkan driver ===
# vulkaninfo | grep -E "driverName|deviceName"

# === GameMode ===
# gamemoded -t

# === MangoHud test ===
# mangohud vkcube