# Kernels
linux-zen
linux-zen-headers
linux-lts
linux-lts-headers

# Core platforms
steam
discord
heroic
protonup-qt
eduke32
# grp files need to be placed in ~/.config/eduke32

# Performance tools
gamemode
lib32-gamemode
mangohud
lib32-mangohud

# Graphics stack (AMD, Vulkan, Proton-safe)
mesa
lib32-mesa
vulkan-radeon
lib32-vulkan-radeon
vulkan-tools

# Controllers & Steam devices
steam-devices
game-devices-udev

# Audio (PipeWire)
pipewire
pipewire-alsa
pipewire-pulse
wireplumber
easyeffects

# Optional Windows compatibility helpers
wine-staging
winetricks

# Optional audio compatibility
lib32-pipewire-jack

# Be in same directory when running the below command
#yay -S --needed $(cat gaming-packages.txt)

# verification checks - run commands separatly one at a time
  # 1. vulkaninfo | grep RADV
        # driverName = AMD RADV
  # 2. gamemoded -t
        # gamemoded is running
  # 3. mangohud glxgears
        # window opens with spinning gears and MangoHud overlay appears (FPS, frame time, etc.)
