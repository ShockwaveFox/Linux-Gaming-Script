# Kernels
linux-zen
linux-zen-headers
linux-lts
linux-lts-headers

# Core platforms
steam
discord
heroic
protonup-qt
eduke32
# grp files need to be placed in ~/.config/eduke32

# Firmware
linux-firmware

# Vulkan extras
vulkan-validation-layers
lib32-vulkan-validation-layers
vulkan-mesa-layers
lib32-vulkan-mesa-layers

# Input / compatibility
sdl2
lib32-sdl2

# Optional but recommended
# gamescope

# Performance tools
gamemode
lib32-gamemode
mangohud
lib32-mangohud
goverlay

# Graphics stack (AMD, Vulkan, Proton-safe)
mesa
lib32-mesa
vulkan-radeon
lib32-vulkan-radeon
vulkan-tools
vulkan-icd-loader
lib32-vulkan-icd-loader

# Controllers & Steam devices
steam-devices
game-devices-udev

# Audio (PipeWire)
pipewire
pipewire-alsa
pipewire-pulse
wireplumber
alsa-utils
easyeffects

# Optional Windows compatibility helpers
# wine-staging
# winetricks

# Optional audio compatibility
# lib32-pipewire-jack

# Be in same directory when running the below command
#yay -S --needed $(cat gaming-packages.txt)

# verification checks - run commands separately one at a time
# 1. Check Vulkan driver (RADV)
      vulkaninfo | grep RADV
            # Expected: driverName = AMD RADV
# 2. Check GameMode daemon
      gamemoded -t
            # Expected: gamemoded is running
# 3. Check MangoHud injection
      # mangohud glxgears
      mangohud vkcube
            # Expected: spinning gears + MangoHud overlay
# 4. Confirm GPU + driver
      vulkaninfo | grep -E "driverName|deviceName"
            # Expected: AMD GPU + RADV

# Enable multi-lib
[multilib]
Include = /etc/pacman.d/mirrorlist
